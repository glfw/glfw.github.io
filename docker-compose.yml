version: "3"

services:
  jekyll_serve:
    image: jekyll/jekyll:4.2.0
    volumes:
      - .:/srv/jekyll
    ports:
      - "4000:4000"
    environment:
      - JEKYLL_ENV=production
    command: ["jekyll", "serve"]
